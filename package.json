{
  "name": "ifv-sistema-gestion",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de gestion de internos IFV con backend Node.js y Google Sheets.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check api/_lib/sheets.js",
    "test": "node --test"
  },
  "engines": {
    "node": "22.x"
  }
}
